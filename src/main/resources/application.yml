spring:
  datasource:
    url: jdbc:mysql:// {DB_HOST:localhost}:{DB_PORT:3306}/{DB_NAME:CNPayment}?createDatabaseIfNotExist=true
    username: root
    password: DB_PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: create-drop
